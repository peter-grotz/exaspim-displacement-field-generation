# exaspim-displacement-field-generation — runtime dependencies
numpy>=1.23
scipy>=1.10          # scipy.spatial.distance.cdist (TPS kernel + Jacobian)
h5py>=3.0            # read Slicer InverseThinPlateSplineKernelTransform .h5
pynrrd>=1.0          # imported as `nrrd` — read/write .nrrd displacement fields

# Optional (only for the ITK-parity unit test; not needed at runtime):
# itk>=5.3
